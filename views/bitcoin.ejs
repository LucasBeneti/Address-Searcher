<!DOCTYPE html>
<html>
    <head>
        <% include header %>
        <meta charset="utf-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" type="text/css" media="screen" href="main.css" />
        <!-- <script src="main.js"></script> -->
        <title>Blockchain Searcher | Bitcoin</title>
    </head>
    <body>
        <div class="container-fluid d-flex align-items-center justify-content-center">
            <div class="content">
                <h3>Aqui você encontra informações sobre endereços de carteiras da main net do Bitcoin</h3>

                <fieldset>
                    <form action="/bitcoin" method="post">
                        <input name="publicKey" type="text" placeholder="Digite algo..." class="ghost-input"  required>
                        <input type="submit" class="ghost-button" value="Search">
                    </form>
                    <hr>
                    <div class="row">
                        <div class="container">
                            <% if(final_balance) { %>
                                <p class="addr_info">Address: <%= address %></p>
                                <p class="addr_info">Balança Final: <%= final_balance %> BTC</p>
                                <p class="addr_info">Total Recebido: <%= total_received%> BTC</p>
                                <p class="addr_info">Total Enviado: <%= total_sent%> BTC</p>
                                <p class="addr_info">Número final de transações: <%= final_n_tx %></p>
                            <% } else { %>
                                <p class="addr_info">Informações pertinentes à carteira especificada aparecerão aqui caso o endereço da Blockchain especificada seja válida...</p>
                            <% } %>
                        </div>
                    </div>
                </fieldset>
            </div>
        </div>    
        <% include footer %>
    </body>
</html>